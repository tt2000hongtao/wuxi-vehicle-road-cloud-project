## 9. 高德 Web JS API/地图 SDK 前端调用口径

后端接口只返回 GCJ-02 坐标，前端地图层统一使用：

- `longitudeGcj02`
- `latitudeGcj02`

前端不得直接使用 CGCS2000 坐标落图。

点位编辑页地图加载顺序：

```text
GET /sites/{siteId}
GET /sites/{siteId}/neighbors
GET /sites/{siteId}/device-items
```

地图视图加载顺序：

```text
GET /sites/map
```
