## 2. 核心结论

合同管理不能按普通“合同台账”设计。应设计成：

```text
合同流图谱
→ 分项包
→ 合同/采购/销售台账
→ 设备级合同明细
→ 设备级前后向合同归属
→ 点位设备安装关联
→ 设备级到货/入库/领料/安装/验收/付款履约
→ 设备级资金流与业主回款联动
```

系统必须支持从任意对象双向追溯：

```text
点位 → 设备 → 合同明细 → 前向合同/后向合同 → 合同流链路 → 上游/下游单位 → 设备级金额/付款/风险

合同 → 分项包 → 设备明细 → 点位安装位置 → 到货/入库/安装/验收/付款节点时间
```
