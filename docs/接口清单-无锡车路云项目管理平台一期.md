# 接口清单：无锡车路云项目管理平台一期

## 1. 接口约定

基础路径建议：

```text
/api/v1
```

返回结构建议：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

分页返回结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [],
    "total": 0,
    "pageNo": 1,
    "pageSize": 20
  }
}
```

## 2. 点位管理接口

### 2.1 点位列表查询

```text
GET /api/v1/sites
```

用途：表格视图、卡片视图共用。点位管理页面不分页展示，前端使用右侧滚动条浏览；后端可按性能需要支持游标或滚动加载。

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| nodeId | string | 否 | NodeID |
| siteName | string | 否 | 点位名称 |
| district | string | 否 | 行政区域 |
| originalDistrict | string | 否 | 原始行政区域，如经开 |
| siteType | string | 否 | 点位类型 |
| signalVendor | string | 否 | 信号机厂商 |
| perceptionSiteType | string | 否 | 感知点位类型 |
| isAdaptiveSite | boolean | 否 | 是否自适应点位 |
| isVariableLaneSite | boolean | 否 | 是否可变车道路口 |
| hasIssue | boolean | 否 | 是否有异常 |
| sortIssueFirst | boolean | 否 | 是否将异常点位排在前面，默认 true |

说明：

- 点位总数按有效点位行数统计，不按 Excel 物理行数统计。
- NodeID 为空、NodeID 重复或关键点位信息不足的导入行不得进入正式点位库，应进入导入错误行或待补全治理清单。
- 经开区在项目过程中已并入新吴区；接口筛选 `district` 使用归并后的标准行政区域，`originalDistrict` 仅用于审计和回查。

返回字段：

- id
- nodeId
- crossId
- siteName
- nodeNameEn
- district
- originalDistrict
- districtNormalizeRule
- siteType
- signalVendor
- perceptionSiteType
- isAdaptiveSite
- isVariableLaneSite
- longitudeGcj02
- latitudeGcj02
- deviceRecordCount
- deviceTypeCount
- installedQuantity
- issueCount
- updatedAt

### 2.1A 删除点位

```text
DELETE /api/v1/sites/{siteId}
```

用途：逻辑删除点位。

删除规则：

- 删除前必须校验点位下是否存在 matched `device_installation_item`。
- 若存在下挂设备，返回 `SITE_HAS_DEVICE_ITEMS`，提示用户先到设备管理删除或迁移该点位下设备。
- 删除成功时写入 `deleted_at`、`deleted_by`、`delete_reason`，并刷新周边点位缓存。

请求体：

```json
{
  "deleteReason": "重复点位清理"
}
```

返回字段：

- id
- deletedAt
- deletedBy

### 2.2 地图点位查询

```text
GET /api/v1/sites/map
```

用途：地图视图查询当前筛选范围点位。

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| district | string | 否 | 行政区域 |
| siteType | string | 否 | 点位类型 |
| hasIssue | boolean | 否 | 是否有异常 |
| minLng | decimal | 否 | 当前地图视野最小经度 GCJ-02 |
| minLat | decimal | 否 | 当前地图视野最小纬度 GCJ-02 |
| maxLng | decimal | 否 | 当前地图视野最大经度 GCJ-02 |
| maxLat | decimal | 否 | 当前地图视野最大纬度 GCJ-02 |

返回字段：

- id
- nodeId
- siteName
- district
- siteType
- signalVendor
- perceptionSiteType
- isAdaptiveSite
- isVariableLaneSite
- longitudeGcj02
- latitudeGcj02
- deviceRecordCount
- deviceTypeCount
- installedQuantity
- issueCount

### 2.3 点位详情

```text
GET /api/v1/sites/{siteId}
```

用途：点位编辑页基础信息。

返回字段：

- site 全量字段
- statistics 统计字段

### 2.4 保存点位

```text
PUT /api/v1/sites/{siteId}
```

用途：保存点位编辑页基础信息。

请求体：

```json
{
  "siteType": "匝道汇入",
  "district": "锡山",
  "originalDistrict": "锡山",
  "nodeId": "10372",
  "crossId": "-1",
  "nodeNameEn": "TaiHuDaDaoBei~YouYiNanLuXi",
  "siteName": "太湖大道北~友谊南路西",
  "signalVendor": "华通",
  "perceptionSiteType": "雷视融合",
  "isAdaptiveSite": false,
  "isVariableLaneSite": false,
  "variableLaneCount": null,
  "variableLaneDirection": ""
}
```

规则：

- NodeID 必须唯一。
- 点位编辑页面默认不编辑或展示原始 CGCS2000 坐标；如需调整坐标，应通过坐标治理流程刷新 GCJ-02 坐标。
- GCJ-02 坐标变化后刷新周边点位缓存。

### 2.5 点位周边点位

```text
GET /api/v1/sites/{siteId}/neighbors
```

用途：点位编辑页嵌入式地图展示当前点位周边 4-5 个点位。

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| limit | int | 否 | 默认 5 |

返回字段：

- rankNo
- siteId
- nodeId
- siteName
- district
- siteType
- longitudeGcj02
- latitudeGcj02
- distanceMeters

### 2.6 重新转换点位坐标

```text
POST /api/v1/sites/{siteId}/coordinate/convert
```

用途：手动重新执行 CGCS2000 到 GCJ-02 转换。

返回字段：

- longitudeGcj02
- latitudeGcj02
- coordinateConvertMethod
- coordinateConvertStatus
- coordinateConvertTime
- 原始 CGCS2000 坐标仅在审计日志中返回

### 2.7 坐标异常列表

```text
GET /api/v1/sites/coordinate-errors
```

用途：坐标异常处理页。

请求参数：

- pageNo
- pageSize
- nodeId
- siteName
- district
- coordinateStatus

返回字段：

- id
- nodeId
- siteName
- district
- longitudeGcj02
- latitudeGcj02
- coordinateConvertStatus
- errorMessage
- updatedAt

说明：坐标治理页面默认显示 GCJ-02 坐标；原始 CGCS2000 坐标仅作为审计字段按需展开。

## 3. 点位设备接口

### 3.1 点位设备清单

```text
GET /api/v1/sites/{siteId}/device-items
```

用途：点位编辑页设备信息页签。

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| groupBy | string | 否 | installPosition/supplier/material |

返回字段：

- id
- nodeId
- siteName
- installPosition
- supplierName
- materialCode
- materialName
- contractName
- brandModel
- unit
- contractQuantity
- deliveredQuantity
- inboundQuantity
- unpackingQuantity
- requisitionQuantity
- installedQuantity
- warehouseQuantity
- deliveryOrderIndex
- inboundOrderIndex
- unpackingOrderIndex
- requisitionOrderIndex
- unpackingIssue

### 3.2 设备安装项详情

```text
GET /api/v1/device-items/{itemId}
```

用途：查看单条设备安装项详情。

返回字段：

- 设备安装位置表全字段
- documents
- relatedSite

### 3.3 已匹配设备分页查询

```text
GET /api/v1/device-items
```

用途：设备管理已匹配设备列表。

请求参数：

- pageNo
- pageSize
- nodeId
- siteName
- installPosition
- supplierName
- materialCode
- materialName
- contractName
- hasUnpackingIssue
- isOnline
- isStatisticsCompleted

返回字段：

- id
- nodeId
- siteName
- installPosition
- supplierName
- materialCode
- materialName
- contractName
- brandModel
- unit
- contractQuantity
- deliveredQuantity
- inboundQuantity
- requisitionQuantity
- installedQuantity
- warehouseQuantity
- unpackingIssue
- matchStatus

## 4. 一点一档接口

### 4.1 点位一点一档

```text
GET /api/v1/sites/{siteId}/archive
```

用途：点位编辑页一点一档页签。

返回结构：

```json
{
  "site": {},
  "coordinate": {},
  "neighbors": [],
  "archiveFolder": {},
  "acceptanceForm": {},
  "acceptanceDeviceItems": [],
  "acceptanceTestItems": [],
  "wiringForm": {},
  "wiringItems": [],
  "photos": [],
  "deviceItems": [],
  "contractSummary": {},
  "documents": [],
  "workOrders": [],
  "issues": [],
  "operationLogs": [],
  "statistics": {}
}
```

### 4.2 一路一档目录列表

```text
GET /api/v1/site-archives/folders
```

用途：查看从一路一档资料目录自动扫描出的路口档案文件夹及匹配状态。

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNo | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |
| archiveName | string | 否 | 文件夹/档案名称 |
| matchStatus | string | 否 | matched/unmatched/manual_matched/ignored |
| district | string | 否 | 归并后行政区域 |

返回字段：

- id
- siteId
- nodeId
- archiveName
- folderPath
- matchedSiteName
- matchStatus
- matchScore
- archiveCompleteness
- missingItems
- importedAt
- updatedAt

### 4.3 扫描一路一档目录

```text
POST /api/v1/site-archives/scan
```

用途：扫描指定目录，自动生成档案目录、资料文件、验收单、接线表、照片记录。

请求体：

```json
{
  "rootPath": "/Users/tt2000/Documents/天安智联/0000无锡市车路云一体化项目/4.项目管理/深化设计/大为/车路云审核通过点位20251014"
}
```

返回字段：

- scannedFolders
- matchedFolders
- unmatchedFolders
- acceptanceFormsParsed
- wiringFormsParsed
- photosImported
- parseFailedCount

### 4.4 手工匹配档案目录到点位

```text
POST /api/v1/site-archives/folders/{folderId}/link-site
```

请求体：

```json
{
  "siteId": 1001,
  "nodeId": "10372",
  "remark": "人工确认点位名称匹配"
}
```

处理规则：

- 更新 `site_archive_folder.match_status=manual_matched`。
- 回写验收单、接线表、照片的 `site_id`。
- 重新计算一点一档完整度。

### 4.5 验收单详情

```text
GET /api/v1/site-archives/folders/{folderId}/acceptance-form
```

返回字段：

- acceptanceForm
- acceptanceDeviceItems
- acceptanceTestItems
- sourceFile

### 4.6 接线表详情

```text
GET /api/v1/site-archives/folders/{folderId}/wiring-form
```

返回字段：

- wiringForm
- wiringItems
- sourceFile

### 4.7 档案照片列表

```text
GET /api/v1/site-archives/folders/{folderId}/photos
```

返回字段：

- id
- fileId
- fileName
- photoType
- shootingDirection
- isCover
- previewUrl

### 4.8 导出一点一档

```text
POST /api/v1/sites/{siteId}/archive/export
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| format | string | 是 | pdf/docx |

返回字段：

- taskId
- downloadUrl
- status

## 5. 数据导入接口

### 5.1 上传点位管理表并预校验

```text
POST /api/v1/import/sites/preview
```

入口：总览页 `导入点位管理表` 按钮。

模板格式参考：

```text
/Users/tt2000/Documents/天安智联/AI/项目管理工具包/Project Data/点位管理表-无锡车路云.xlsx
```

请求：

- multipart file

返回字段：

- importTaskId
- totalRows
- validRows
- invalidRows
- duplicateNodeIdCount
- coordinateErrorCount
- dictionaryErrorCount
- pendingCompletionRows
- previewRows

### 5.2 确认导入点位管理表

```text
POST /api/v1/import/sites/confirm
```

请求体：

```json
{
  "importTaskId": "xxx"
}
```

返回字段：

- importedRows
- failedRows
- coordinateConvertedRows
- coordinateFailedRows
- pendingCompletionRows

### 5.3 上传设备安装位置表并预校验

```text
POST /api/v1/import/device-items/preview
```

入口：总览页 `导入设备管理表` 按钮。

模板格式参考：

```text
/Users/tt2000/Documents/天安智联/AI/项目管理工具包/Project Data/设备安装位置表.xlsx
```

用途：导入设备点位分布信息，按 NodeID 匹配到点位，并将未匹配记录进入未匹配池。

请求：

- multipart file

返回字段：

- importTaskId
- sheet1TotalRows
- matchedRows
- matchFailedRows
- unmatchedSheetRows
- unpackingIssueRows
- quantityErrorRows
- previewRows

### 5.4 确认导入设备安装位置表

```text
POST /api/v1/import/device-items/confirm
```

请求体：

```json
{
  "importTaskId": "xxx"
}
```

返回字段：

- importedMatchedRows
- importedUnmatchedRows
- createdGovernanceRows
- unpackingIssueRows

## 6. 未匹配设备治理接口

### 6.1 未匹配设备查询

```text
GET /api/v1/device-unmatched-items
```

请求参数：

- pageNo
- pageSize
- supplierName
- installPosition
- contractSection
- materialName
- contractName
- governanceStatus

说明：一期初始化展示 `未匹配` 工作表中的 2447 条记录，支持按物料、供应商、合同板块治理到点位、仓库、机房或服务项。

返回字段：

- id
- sourceItemId
- installPosition
- supplierName
- materialCode
- materialName
- contractName
- brandModel
- unit
- contractQuantity
- deliveredQuantity
- inboundQuantity
- requisitionQuantity
- installedQuantity
- warehouseQuantity
- governanceStatus

### 6.2 关联点位

```text
POST /api/v1/device-unmatched-items/{id}/link-site
```

请求体：

```json
{
  "siteId": 1001,
  "nodeId": "10372",
  "remark": "人工匹配到太湖大道北~友谊南路西"
}
```

处理规则：

- 更新未匹配治理状态为 `已关联点位`。
- 更新来源设备安装项 `site_id`、`node_id`、`match_status=matched`。
- 重新计算对应点位设备统计。

### 6.3 标记为非点位资产

```text
POST /api/v1/device-unmatched-items/{id}/mark
```

请求体：

```json
{
  "targetType": "server_room",
  "targetId": 1,
  "governanceStatus": "已关联机房",
  "remark": "交警支队机房设备"
}
```

targetType 可选：

- server_room
- warehouse
- vehicle
- service
- ignored

### 6.4 批量治理

```text
POST /api/v1/device-unmatched-items/batch-governance
```

请求体：

```json
{
  "ids": [1, 2, 3],
  "targetType": "service",
  "governanceStatus": "软件服务项",
  "remark": "合同服务类条目，不纳入点位设备统计"
}
```

## 7. 资料接口

### 7.1 查询对象资料

```text
GET /api/v1/documents
```

请求参数：

- objectType
- objectId
- fileCategory

### 7.2 上传资料

```text
POST /api/v1/documents
```

请求：

- multipart file
- objectType
- objectId
- fileCategory
- remark

### 7.3 删除资料

```text
DELETE /api/v1/documents/{documentId}
```

## 8. 合同管理接口

### 8.1 合同驾驶舱

```text
GET /api/v1/contracts/dashboard
```

返回字段：

- totalContractAmount
- mobileOutboundAmount
- tiananOutboundAmount
- mobileToTiananAmount
- originalFactoryAmount
- grossProfitAmount
- signedContractCount
- pendingSignContractCount
- matchedDeviceItemCount
- unmatchedContractItemCount
- unmatchedDeviceItemCount
- riskCount

### 8.2 导入合同测算表预校验

```text
POST /api/v1/import/contracts/preview
```

请求：

- multipart file：`无锡车路云一体化采购价格测算表202500920.xlsx`

处理内容：

- 读取 `合同流`
- 读取 `合同签署计划`
- 读取 `分项报价汇总表`
- 读取各分项明细 sheet

返回字段：

- importTaskId
- flowLineCount
- packageCount
- contractItemCount
- partyCount
- detailSheetCount
- parseWarningCount
- previewFlowLines

### 8.3 确认导入合同测算表

```text
POST /api/v1/import/contracts/confirm
```

请求体：

```json
{
  "importTaskId": "xxx"
}
```

返回字段：

- importedFlowLines
- importedFlowNodes
- importedFlowEdges
- importedPackages
- importedContractItems
- importedSignPlans

### 8.3.1 扫描前向/后向合同文件目录

```text
POST /api/v1/import/contracts/documents/scan
```

用途：扫描天安前项销售、后项采购盖章合同目录，生成合同文件索引，并尝试 OCR 识别合同编号、甲乙方、金额、签署日期和合同清单。

请求体：

```json
{
  "contractDirection": "front_sales",
  "rootPath": "/Users/tt2000/Documents/天安智联/0000无锡市车路云一体化项目/0.合同/合同管理/前项天安销售(1)"
}
```

`contractDirection` 可选：

- `front_sales`：天安前向销售合同
- `back_purchase`：天安后向采购合同

返回字段：

- scannedFiles
- parsedFiles
- parseFailedFiles
- createdContracts
- suspectedContracts
- ocrWarningCount

### 8.3.2 合同主体归一

```text
POST /api/v1/contract-parties/normalize
```

用途：执行合同主体名称归一，例如尚行、上海帆一尚行、帆一归一为上汽帆一。

请求体：

```json
{
  "rules": [
    {
      "standardPartyName": "上汽帆一",
      "aliases": ["尚行", "上海帆一尚行科技有限公司", "帆一"]
    }
  ]
}
```

### 8.4 合同流列表

```text
GET /api/v1/contract-flow-lines
```

请求参数：

- pageNo
- pageSize
- itemNo
- projectItemName
- splitPackageName
- partyName
- flowType
- detailSheetName

返回字段：

- id
- itemNo
- projectItemName
- bidAmount
- splitPackageName
- splitPackageAmount
- flowPathText
- flowType
- originalFactoryAmount
- grossProfitAmount
- discountRate
- primarySupplier
- detailSheetName

### 8.5 合同流图谱

```text
GET /api/v1/contract-flow-graph
```

请求参数：

- itemNo
- partyName
- flowType
- packageId

返回结构：

```json
{
  "nodes": [
    {
      "id": 1,
      "partyName": "移动",
      "partyType": "总集",
      "isConsortiumMember": true
    }
  ],
  "edges": [
    {
      "fromPartyId": 1,
      "toPartyId": 2,
      "amount": 1000000,
      "edgeType": "mobile_to_tianan",
      "flowLineIds": [10, 11]
    }
  ]
}
```

### 8.6 分项包列表

```text
GET /api/v1/contract-packages
```

请求参数：

- pageNo
- pageSize
- packageNo
- packageName
- packageCategory
- primarySupplier

返回字段：

- id
- packageNo
- packageName
- packageCategory
- subtotalAmount
- totalAmount
- originalFactoryAmount
- grossProfitAmount
- discountRate
- primarySupplier
- detailSheetName
- canLinkSite
- canLinkDevice

### 8.7 合同明细列表

```text
GET /api/v1/contract-items
```

请求参数：

- pageNo
- pageSize
- packageId
- sourceSheetName
- itemType
- itemName
- brandModel
- flowPackageName
- matchStatus

返回字段：

- id
- packageId
- sourceSheetName
- sourceRowNo
- itemNo
- itemType
- itemName
- brandModel
- deploymentLocation
- unit
- quantity
- unitPrice
- amount
- taxRate
- flowPackageName
- originalFactoryUnitPrice
- originalFactoryAmount
- grossProfitAmount
- frontContractId
- backContractId
- originalSupplierPartyId
- ownerBackflowRequired
- matchStatus

### 8.7.1 合同明细主体角色

```text
GET /api/v1/contract-items/{contractItemId}/contract-roles
```

用途：查看单条设备/服务明细对应的前向销售、后向采购、原厂供应、业主回流等角色。

返回字段：

- id
- contractItemId
- roleType
- contractId
- partyId
- partyName
- sourceBasis
- allocationMethod
- allocatedQuantity
- allocatedAmount
- confidenceScore

### 8.7.2 保存合同明细主体角色

```text
POST /api/v1/contract-items/{contractItemId}/contract-roles
```

请求体：

```json
{
  "roleType": "back_purchase",
  "contractId": 2001,
  "partyId": 301,
  "allocationMethod": "by_quantity",
  "allocatedQuantity": 10,
  "allocatedAmount": 150000
}
```

### 8.8 合同明细匹配设备

```text
POST /api/v1/contract-items/{contractItemId}/match-devices
```

用途：对单条合同明细执行自动设备匹配。

返回字段：

- contractItemId
- candidateCount
- autoMatchedCount
- suspectedCount
- candidates

候选字段：

- deviceInstallationItemId
- nodeId
- siteName
- installPosition
- materialName
- brandModel
- supplierName
- matchScore
- matchBasis

### 8.9 确认合同设备匹配

```text
POST /api/v1/contract-item-device-matches/confirm
```

请求体：

```json
{
  "contractItemId": 1001,
  "deviceInstallationItemId": 2001,
  "matchedQuantity": 1,
  "matchStatus": "confirmed",
  "remark": "物料名称和品牌型号一致"
}
```

### 8.10 点位合同穿透

```text
GET /api/v1/sites/{siteId}/contract-trace
```

用途：点位编辑页展示该点位设备对应的合同链路。

返回结构：

```json
{
  "site": {},
  "deviceContracts": [
    {
      "deviceInstallationItemId": 1,
      "materialName": "智能抱杆箱",
      "installPosition": "东",
      "contractItem": {},
      "flowPathText": "移动->浪潮->天安->大为",
      "package": {},
      "contract": {},
      "frontContract": {},
      "backContract": {},
      "fulfillmentEvents": [],
      "cashFlows": [],
      "fulfillment": {}
    }
  ]
}
```

### 8.11 合同台账列表

```text
GET /api/v1/contracts
```

请求参数：

- pageNo
- pageSize
- contractName
- contractType
- upstreamPartyId
- downstreamPartyId
- contractStatus
- plannedSignDateFrom
- plannedSignDateTo

### 8.12 合同详情

```text
GET /api/v1/contracts/{contractId}
```

返回结构：

```json
{
  "contract": {},
  "flowLines": [],
  "flowEdges": [],
  "items": [],
  "deviceMatches": [],
  "paymentNodes": [],
  "documents": [],
  "risks": []
}
```

### 8.13 合同风险列表

```text
GET /api/v1/contract-risks
```

请求参数：

- pageNo
- pageSize
- riskType
- severity
- partyName
- packageNo
- status

风险类型：

- flow_break
- amount_mismatch
- contract_item_unmatched_device
- device_unmatched_contract
- quantity_mismatch
- negative_profit
- sign_delay

### 8.14 设备级履约事件列表

```text
GET /api/v1/contract-items/{contractItemId}/fulfillment-events
```

请求参数：

- deviceInstallationItemId
- nodeId
- eventType
- eventStatus

返回字段：

- id
- contractItemId
- deviceInstallationItemId
- siteId
- nodeId
- eventType
- eventStatus
- plannedTime
- actualTime
- eventQuantity
- evidenceObjectType
- evidenceObjectId

### 8.15 保存设备级履约事件

```text
POST /api/v1/contract-items/{contractItemId}/fulfillment-events
```

请求体：

```json
{
  "deviceInstallationItemId": 2001,
  "eventType": "install",
  "eventStatus": "done",
  "actualTime": "2026-05-16T10:00:00+08:00",
  "eventQuantity": 1,
  "evidenceObjectType": "document",
  "evidenceObjectId": 9001
}
```

### 8.16 设备级资金流列表

```text
GET /api/v1/contract-items/{contractItemId}/cash-flows
```

请求参数：

- deviceInstallationItemId
- contractId
- cashFlowDirection
- cashFlowStatus
- ownerBackflowRequired

返回字段：

- id
- contractItemId
- deviceInstallationItemId
- contractId
- cashFlowDirection
- fromPartyId
- toPartyId
- plannedAmount
- actualAmount
- plannedDate
- actualDate
- triggerEventId
- upstreamCashFlowId
- ownerBackflowRequired
- ownerBackflowStatus
- cashFlowStatus

### 8.17 生成设备级资金流计划

```text
POST /api/v1/contracts/{contractId}/cash-flows/generate
```

用途：根据合同付款节点、合同明细、设备匹配和履约事件，生成设备级应收/应付/质保金/业主回流计划。

请求体：

```json
{
  "allocationMethod": "by_quantity",
  "respectUpstreamReceipt": true,
  "includeOwnerBackflow": true
}
```

## 9. 高德 Web JS API/地图 SDK 前端调用口径

后端接口只返回 GCJ-02 坐标，前端地图层统一使用：

- `longitudeGcj02`
- `latitudeGcj02`

前端不得直接使用 CGCS2000 坐标落图。

点位编辑页地图加载顺序：

```text
GET /sites/{siteId}
GET /sites/{siteId}/neighbors
GET /sites/{siteId}/device-items
```

地图视图加载顺序：

```text
GET /sites/map
```

## 10. 错误码建议

| 错误码 | 含义 |
|---|---|
| SITE_NODE_ID_DUPLICATED | NodeID 重复 |
| SITE_NOT_FOUND | 点位不存在 |
| COORDINATE_INVALID | 坐标格式错误 |
| COORDINATE_CONVERT_FAILED | 坐标转换失败 |
| DEVICE_ITEM_NOT_FOUND | 设备安装项不存在 |
| DEVICE_ITEM_ALREADY_MATCHED | 设备安装项已匹配 |
| IMPORT_FILE_INVALID | 导入文件格式错误 |
| IMPORT_FIELD_MISSING | 导入字段缺失 |
| UNMATCHED_ITEM_NOT_FOUND | 未匹配设备不存在 |
| SITE_HAS_DEVICE_ITEMS | 点位下挂有设备，需先删除或迁移设备后才能删除点位 |

## 11. 文档资产管理接口

文档资产接口遵循“元数据查询、原件下载、业务对象关联、批量导入、解析质检分离”的原则。大文件不进入前端内嵌数据，前端只展示后端返回的元数据和预览地址。

### 11.1 文档上传登记

```text
POST /api/v1/documents/upload
```

用途：上传或登记单个文件，计算 `sha256`，写入文档元数据，并可同时建立业务对象关联。

请求体采用 `multipart/form-data`：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| file | file | 是 | 文件原件 |
| projectCode | string | 是 | 项目编码 |
| documentCategory | string | 是 | 文档分类 |
| documentSubcategory | string | 否 | 文档子类 |
| objectType | string | 否 | 关联对象类型 |
| objectId | string | 否 | 关联对象 ID，例如 NodeID、contractId |
| uploadedBy | string | 否 | 上传人 |

返回字段：

- id
- documentNo
- fileName
- fileExt
- fileSizeBytes
- sha256
- storageProvider
- storageKey
- currentVersionNo
- reviewStatus
- parseStatus

### 11.2 文档列表查询

```text
GET /api/v1/documents
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| projectCode | string | 否 | 项目编码 |
| keyword | string | 否 | 文件名、编号、摘要关键字 |
| documentCategory | string | 否 | 文档分类 |
| objectType | string | 否 | 关联对象类型 |
| objectId | string | 否 | 关联对象 ID |
| reviewStatus | string | 否 | draft/effective/archived/voided/pending_review |
| parseStatus | string | 否 | pending/success/failed/skipped |
| qualityStatus | string | 否 | normal/warning/error |
| pageNo | int | 否 | 页码 |
| pageSize | int | 否 | 每页数量 |

返回字段：

- id
- documentNo
- fileName
- documentCategory
- documentSubcategory
- fileExt
- fileSizeBytes
- currentVersionNo
- reviewStatus
- archiveStatus
- parseStatus
- qualityStatus
- relationCount
- uploadedAt
- uploadedBy

### 11.3 文档详情

```text
GET /api/v1/documents/{id}
```

返回字段：

- document 基础元数据
- versions 版本列表
- relations 业务对象关联
- parseResult 最新解析结果
- qualityIssues 质检问题
- accessSummary 访问摘要

### 11.4 文档下载

```text
GET /api/v1/documents/{id}/download
```

用途：通过后端权限校验后返回文件流，或返回对象存储预签名 URL。

返回方式：

- 本地/NAS：文件流。
- 对象存储：预签名 URL 或后端代理流。

### 11.5 新增文档业务关系

```text
POST /api/v1/documents/{id}/relations
```

请求体：

```json
{
  "objectType": "site",
  "objectId": "10372",
  "objectDisplayName": "太湖大道北~友谊南路西",
  "relationType": "acceptance_archive",
  "matchSource": "manual"
}
```

返回字段：

- relationId
- documentId
- objectType
- objectId
- relationType
- createdAt

### 11.6 查询业务对象关联文档

```text
GET /api/v1/objects/{objectType}/{objectId}/documents
```

用途：点位、设备、合同、地图资产、出入库、运维等对象详情页资料页签使用。

返回字段：

- id
- documentNo
- fileName
- documentCategory
- fileExt
- fileSizeBytes
- currentVersionNo
- reviewStatus
- parseStatus
- qualityStatus
- relationType
- uploadedAt

### 11.7 创建批量导入任务

```text
POST /api/v1/document-import-batches
```

请求体：

```json
{
  "projectCode": "wuxi-cv2x",
  "sourceRoot": "/path/to/source",
  "importMode": "scan_only",
  "categoryHint": "contract"
}
```

返回字段：

- batchNo
- status
- sourceRoot
- fileCount
- totalSizeBytes
- duplicateFileCount
- unlinkedFileCount
- reportUrl

### 11.8 查询批量导入任务

```text
GET /api/v1/document-import-batches/{id}
```

返回字段：

- batch 基础信息
- summary 统计摘要
- files 扫描文件样例
- errors 错误与待治理项

### 11.9 触发文档解析

```text
POST /api/v1/documents/{id}/parse
```

请求体：

```json
{
  "parserMode": "lightweight",
  "force": false
}
```

返回字段：

- parseTaskId
- parseStatus
- startedAt

### 11.10 查询文档版本

```text
GET /api/v1/documents/{id}/versions
```

返回字段：

- versionNo
- sha256
- fileSizeBytes
- storageKey
- changeReason
- createdAt
- createdBy

### 11.11 文档资产错误码

| 错误码 | 含义 |
|---|---|
| DOCUMENT_NOT_FOUND | 文档不存在 |
| DOCUMENT_STORAGE_NOT_FOUND | 存储对象不存在 |
| DOCUMENT_SHA256_DUPLICATED | 文件哈希重复 |
| DOCUMENT_RELATION_INVALID | 业务对象关联无效 |
| DOCUMENT_OBJECT_NOT_FOUND | 关联业务对象不存在 |
| DOCUMENT_PARSE_FAILED | 文档解析失败 |
| DOCUMENT_ACCESS_DENIED | 无文档访问权限 |
| DOCUMENT_IMPORT_BATCH_NOT_FOUND | 批量导入任务不存在 |
| DOCUMENT_STORAGE_KEY_INVALID | 存储键非法 |
